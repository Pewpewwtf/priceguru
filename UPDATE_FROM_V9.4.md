# Update from v9.4 to v9.5.2

Replace the repository root with the files from this archive and push. Keep all existing ENV variables. `OZON_PROXY_URL` should stay configured. After deploy, `/api/health` must show `version: 9.5.2` and `ozonProxy: true`.
